# Install

``` html
<script src="https://unpkg.com/torus-web-browser-version@latest"></script>
```

# Usage

``` javascript
const torus = new torusWeb.default());
await torus.init({});
await torus.login();
```